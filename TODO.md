* The photo location and date should be in the bottom right of the screen, not the bottom right of the image (ex: in cases where it is a portrait image with black borders)
* Filter the cast receiver list to just those that accept video / image output (right now it shows audio only devices as well)
* Update the interface to support multiple output devices (with unique lists). We should replicate most the interface for each (so they can uniquely be controlled, etc)
